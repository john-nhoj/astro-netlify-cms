[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/admin/#/*"
  to = "/admin/#/*"
  status = 200
  conditions = {Role = ["admin"]}
  force = true

[dev]
  autoLaunch = false
